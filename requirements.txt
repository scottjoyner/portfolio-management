# Portfolio Management System Dependencies
# Install with: pip install -r requirements.txt
#
# Canonical production dependencies live in deploy/requirements.txt and are
# included below. The additional entries are root-level tooling deps used by
# the local .venv (prediction-market execution, data sources, utilities).

-r deploy/requirements.txt

# Core data sources
yfinance>=0.2.36
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0

# Prediction-market execution (Polymarket CLOB, on-chain order signing)
py-clob-client>=0.34.0
eth-account>=0.13.0
web3>=7.0.0
