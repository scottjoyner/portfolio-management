"""Repository automation and operational command modules.

Making ``scripts`` a regular package prevents test-only packages with the same
name from shadowing production commands during isolated imports.
"""
