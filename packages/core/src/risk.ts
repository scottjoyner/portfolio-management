import {RiskDecision} from "./types.js"; export function preTradeRisk(edgeBps:number,notional:number,cfg:{minEdgeBps:number;maxNotional:number;killSwitch:boolean;}):RiskDecision{const reasons:string[]=[]; if(cfg.killSwitch) reasons.push("kill_switch"); if(edgeBps<cfg.minEdgeBps) reasons.push("edge_too_low"); if(notional>cfg.maxNotional) reasons.push("notional_limit"); return {approved:reasons.length===0,reasons};}