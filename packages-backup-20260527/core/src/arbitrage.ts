import {OrderBook,ArbitrageOpportunity} from "./types.js";import {bestAsk} from "./orderbook.js";import {bps} from "./fees.js";import {USD} from "./money.js"; export function calcArb(pairId:string,a:OrderBook,b:OrderBook,feeBps=20,slipBps=20):ArbitrageOpportunity|null{const ay=bestAsk(a,"yes");const bn=bestAsk(b,"no");if(!ay||!bn) return null;const raw=ay.priceMicros+bn.priceMicros;const costs=raw+bps(raw,feeBps)+bps(raw,slipBps);const edge=USD-costs; if(edge<=0) return null; return {pairId,direction:"buy_yes_a_buy_no_b",totalCostMicros:costs,edgeBps:Math.floor(edge*10_000/USD),size:Math.min(ay.size,bn.size)};}